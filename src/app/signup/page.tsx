import SignUp from '@/components/pages/SignUp';
import React from 'react';

const page: React.FC = () => {
    return (
        <div>
            <SignUp />
        </div>
    );
};

export default page;